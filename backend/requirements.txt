# Full deps (API + worker). Use requirements-api.txt for the API container.
-r requirements-worker.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
websockets>=13.0,<15.0

# Async task queue
celery==5.4.0
redis==5.0.7

# Database — SQLAlchemy async (SQLite dev → PostgreSQL prod via DATABASE_URL)
sqlalchemy[asyncio]==2.0.31
aiosqlite==0.20.0          # SQLite async driver
asyncpg==0.29.0            # PostgreSQL async driver (prod)

# LLM unified interface (all cloud LLM calls go through this)
litellm==1.41.27

# Anthropic SDK (direct — for vision OCR endpoint)
anthropic>=0.40.0

# Google Gemini SDK (direct — for Gemini vision OCR endpoint)
google-genai>=1.0.0

# Azure Document Intelligence (not an LLM — SDK used directly per spec)
azure-ai-formrecognizer==3.3.3

# OCR
paddlepaddle==2.6.2
paddleocr==2.7.3
easyocr==1.7.2

# PDF → image rendering
PyMuPDF==1.24.5

# NLP / local models
transformers==4.41.2
torch==2.3.1
sentencepiece==0.2.0

# Experiment tracking
mlflow==2.13.2

# Config management
pydantic-settings==2.3.0
python-dotenv==1.0.1

# HTTP client
httpx>=0.28.1,<1.0.0

# Image processing
Pillow==10.3.0
numpy==1.26.4

# Oracle DB stub — read-only, training data only, NOT part of live pipeline
# Requires Oracle Instant Client installed separately
# cx-Oracle==8.3.0
